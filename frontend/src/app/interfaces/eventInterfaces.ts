// src/interfaces/eventInterfaces.ts

export interface Event {
  id: number;
  title: string;
  imageUrl: string;
  description: string;
  // Add any other properties that your event objects might have
}
